# Claude Code worktrees (temporary, not part of project)
.claude/worktrees/

# Impeccable design skill internal data
.impeccable/

# Node dev dependencies (icon generation)
node_modules/
package-lock.json
package.json
